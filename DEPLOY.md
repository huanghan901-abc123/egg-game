# 发布到手机

这是纯静态网页，不需要安装依赖或数据库。把整个项目文件夹上传到任意静态托管服务即可，例如 GitHub Pages、Netlify 或 Vercel。

1. 保持 `index.html`、`src/`、`assets/` 和 `service-worker.js` 的相对位置不变。
2. 使用 HTTPS 地址打开网站根目录；根页面会自动进入游戏。
3. 在 Android Chrome 或 iPhone Safari 中打开后，选择“添加到主屏幕”，即可像 App 一样启动。

注意：学习档案、进度和错题本保存在浏览器本机。清除网站数据或更换设备会丢失这些数据。
